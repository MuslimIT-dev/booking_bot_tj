{
  "name": "booking-bot",
  "version": "1.0.0",
  "main": "dist/bot.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/bot.js",
    "dev": "nodemon src/bot.ts",
    "migrate": "prisma migrate dev",
    "deploy": "prisma migrate deploy && npm run build && pm2 restart booking-bot"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@telegraf/session": "^2.0.0-beta.6",
    "date-fns": "^2.30.0",
    "dotenv": "^16.6.1",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "nodemon": "^3.0.1",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
